#taking 2 digit numbers from user, and type conversion of the input, then finally adding those numbers.
int_to_addition = input("enter 2 digit number, get an addition ")
first_digit = int(int_to_addition[0])
second_digit = int(int_to_addition[1])
total = first_digit + second_digit
print(total)